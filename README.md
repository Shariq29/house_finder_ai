# house_finder_ai
This is a simple application built on top of llama-index

## Steps to tun the bot:
1). In env file, add your gemini api key under the name GEMINI_API_KEY

2). Install the libraries mentioned in requirements.txt

3). Run the main.py file (This will infer data from house-price.xlsx)